package src.etch.types;

public interface InternalType extends Type {

	/* Used to represent a type which may not appear in a Promela specification
	 * directly - a ProductType or a RecursiveType.
	 */
}
