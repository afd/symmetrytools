package src.etch.types;

public interface VisibleType extends Type {

	/* A VisibleType is a type that can actually be written in a Promela specification,
	 * and thus may appear as e.g. an array element type, or ProductType entry.
	 */
}
