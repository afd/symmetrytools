/* This file was generated by SableCC (http://www.sablecc.org/). */

package src.promela.node;

import src.promela.analysis.*;

@SuppressWarnings("nls")
public final class ATimeoutFactor extends PFactor
{
    private TTimeout _timeout_;

    public ATimeoutFactor()
    {
        // Constructor
    }

    public ATimeoutFactor(
        @SuppressWarnings("hiding") TTimeout _timeout_)
    {
        // Constructor
        setTimeout(_timeout_);

    }

    @Override
    public Object clone()
    {
        return new ATimeoutFactor(
            cloneNode(this._timeout_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseATimeoutFactor(this);
    }

    public TTimeout getTimeout()
    {
        return this._timeout_;
    }

    public void setTimeout(TTimeout node)
    {
        if(this._timeout_ != null)
        {
            this._timeout_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._timeout_ = node;
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._timeout_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._timeout_ == child)
        {
            this._timeout_ = null;
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._timeout_ == oldChild)
        {
            setTimeout((TTimeout) newChild);
            return;
        }

        throw new RuntimeException("Not a child.");
    }
}
